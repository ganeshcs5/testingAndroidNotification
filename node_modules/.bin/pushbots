#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pushbots-cli/bin/pushbots" "$@"
  ret=$?
else 
  node  "$basedir/../pushbots-cli/bin/pushbots" "$@"
  ret=$?
fi
exit $ret
